strict-peer-dependencies=false
auto-install-peers=true

# node-linker 决定 pnpm 如何生成 node_modules 结构：
# - hoisted: 类似 yarn/npm，把依赖提升到顶层 node_modules，更兼容传统工具（推荐）
# - node-modules: 每个包有完整 node_modules，隔离性强，但性能较差
node-linker=hoisted